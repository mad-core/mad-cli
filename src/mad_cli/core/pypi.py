"""Latest-version lookup via the PyPI JSON API (urllib, stdlib only). Contract stub."""

from __future__ import annotations


def latest_version(package: str, timeout_s: float = 5.0) -> str | None:
    raise NotImplementedError
